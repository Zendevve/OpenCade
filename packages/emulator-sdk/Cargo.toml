[package]
name = "opencade-emulator-sdk"
version = "0.1.0"
edition = "2021"
description = "OpenCade emulator adapter SDK — trait for pluggable emulator backends"
license = "Apache-2.0"
repository = "https://github.com/opencade/opencade"
rust-version = "1.78"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "1"
